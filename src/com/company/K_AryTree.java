package com.company;

public class K_AryTree {

    //K-Ary Tree
    //https://practice.geeksforgeeks.org/problems/k-ary-tree1235/1#
    /*
    long long karyTree(int k, int m) {
        //return pow(k, m ) but % mod in each step so
        long long leaf=1;
        // root itself is one leaf if height is 0
        // also only
        for( int i=0; i<m; i++){
            leaf= (leaf*k)%1000000007;
        }
        return leaf;
    }
     */
}
