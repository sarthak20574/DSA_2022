package com.company;

public class Smallest_greater_elements_in_whole_array {
    /*
    int* greaterElement(int arr[], int n)
{

    // set keeps the numbers in sorted order then binary search


    //upperbound gives us the next greater element

    set<int> x;

    for( int i=0 ;i<n; i++){
        x.insert(arr[i]);
    }

    for( int i=0;i<n;i++){

       auto ans= x.upper_bound(arr[i]);

       if( ans!=x.end()) arr[i]= *ans;
       else{
           arr[i]=-10000000;
       }

    }
    return arr;
}

     */
}
