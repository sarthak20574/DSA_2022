package com.company.Unboxing_Interview_Series_GFG;

public class Arithmetic_Number {
    //Arithmetic Number
    /*
     int inSequence(int A, int B, int C){
        if(C==0 && B-A ==0 || B==A) return 1;
        if( B-A <=0 && C<0 || B-A>0 && C>0)
        return (B-A)%C==0;
        else return 0;
    }
     */
}
